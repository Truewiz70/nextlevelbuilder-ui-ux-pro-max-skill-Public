# Dashboard (Next.js)

Scaffolded in **Phase 7 (Reporting)**. Reserved here so the monorepo layout is
stable from day one.

Planned:
- Next.js (App Router, TypeScript), deployed on Vercel
- Typed API client generated from the FastAPI OpenAPI spec
  (`packages/shared-types`)
- Views: call list + transcripts, analytics KPIs, tenant configuration,
  knowledge-base editor, callback queue
